using System;

namespace Refge
{
    public static class Refge
    {
        public static string GetVersion(string version)
        {
            return "v" + version;
        }

        public static string GetAVersion(string version)
        {
            return "a" + version;
        }
    }
}
